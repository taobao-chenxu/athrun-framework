If you want to run test cases that contain scenes such as interrupted by an incoming call or sending key events across applications, 
please run the debug.bat in tool\debug before running your test cases as Android JUnit Test. 
In this project, that is DeviceTest.